import * as chai from 'chai';

const assert = chai.assert;

describe('蔡徐坤', () => {
  it('会唱', () => {
    assert(1 === 1)
  });
  it('会跳', () => {
    assert(2 === 2)
  });
  it('会rap', () => {
    assert(3 === 3)
  });
  it('会篮球', () => {
    assert(4 === 4)
  })
})